
Add(3x^2+2x-5);
Mult(2x+1);
Add(-x^3+4x);           % Adds the polynomial -x^3 + 4x to the current value in the accumulator
Eval(2);
Sub(5x^2-3x+7);
Div(x-2);
Mult(3);
Eval(1);                 % [sgds]

[
Multiline comment
before performing differentiation
]

Cmps(x-2);              % Composes the current polynomial with (x - 2)

Diff(x^3-2x^2-x+3);
Add(10);

% End of test command file
